import { Box, styled } from "@mui/material";

export const ChartStyledBox = styled(Box)({
    p: 3,
    pb: 1,
    display: 'flex',
    flexDirection: 'column',
    transition: 'all .3s ease',
    gap: 2,
});